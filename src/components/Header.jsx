import React from 'react'
import reactLogo from './../logo.svg'

function Header(){
    return(
        <header className='header'>
            <img src={reactLogo} />
        </header>
    );
}

export default Header;